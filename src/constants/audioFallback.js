/**
 * Remote MP3 used only as a second <source> on <audio> if the file in /public/audio/rhymes fails.
 * (Same test clip as bundled files — helps when PUBLIC_URL paths are misconfigured.)
 */
export const RHYME_MUSIC_REMOTE_FALLBACK =
  'https://archive.org/download/testmp3testfile/mpthreetest.mp3';
